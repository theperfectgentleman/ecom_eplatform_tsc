import React from 'react';

// Placeholder component (file previously empty / causing lint issues if referenced)
// Update with actual implementation when AI agent feature is developed.
const FloatingAIAgent: React.FC = () => null;

export default FloatingAIAgent;
